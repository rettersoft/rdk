{
    "name": "@retter/rdk",
    "version": "2.0.32",
    "description": "Retter Development Kit",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "clean": "rm -rf dist",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc -b",
        "prepare": "npm run build",
        "bump": "bump patch package.json --commit --tag",
        "clean-install": "npm cache clean --force && rm -rf node_modules package-lock.json && npm install"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/rettersoft/rdk.git"
    },
    "author": "Retter",
    "license": "ISC",
    "dependencies": {
        "axios": "1.10.0"
    },
    "devDependencies": {
        "@jsdevtools/version-bump-prompt": "6.1.0",
        "@types/node": "17.0.7",
        "typescript": "4.1.3"
    }
}
